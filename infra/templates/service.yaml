apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}-service
  namespace: {{.Values.namespace}}
spec:
  selector:
    name: {{.Release.Name}}-ai-dev-log
  ports:
    - port: {{ .Values.deployment.containerPort }}
      targetPort: {{ .Values.deployment.containerPort }}
